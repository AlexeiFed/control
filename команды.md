После правки .env на сервере процесс уже подхватит переменные только после рестарта (unit читает EnvironmentFile= при старте):

ssh root@85.209.9.29 'systemctl restart vityaz-erp.service && systemctl status vityaz-erp.service --no-pager'


Деплой:
bash scripts/deploy.sh